[package]
name = "plugin_forensically_ela"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin_api = { path = "../plugin_api" }
image = "0.25"
jpeg-encoder = "0.7.0"
